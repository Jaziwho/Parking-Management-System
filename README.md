# Parking-Management-System
A C++ DSA-based simulation of urban parking management using arrays, linked lists, stacks, and queues. Implements zone-based allocation with same-zone preference and cross-zone fallback. Features complete request lifecycle (REQUESTED→ALLOCATED→OCCUPIED→RELEASED) with state machine validation.
